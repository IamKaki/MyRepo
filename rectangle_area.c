#include <stdio.h>

/**
  * main - programm to find area of rectangle
  * Return: - Always 0
  */

int main(void)
{
	int h = 23, l = 14;

	printf("The area of the rectangle is %d * %d = %d.\n", h, l, h * l);

	return (0);
}
